# Frontend Environment Variables for Local Development
# 
# ⚠️ IMPORTANT: This file is ONLY used for local development!
# 
# - Local dev:   frontend/.env → set-version.js → runtime-config.json
# - Azure:       Bicep (infra/main.bicep) → Container env vars → merge-runtime-config.js
# 
# To change JWT timeout, update backend/src/main/resources/application-local.properties:
#   jwt.access-token-expiration-minutes=4
# 
# Frontend automatically fetches JWT timeout from backend at /api/config/environmentProperties
# 
# See docs/CONFIGURATION-MANAGEMENT.md for details.

# API Base URL
API_BASE_URL=http://localhost:8080

# Note: JWT timeout values are fetched from backend dynamically
# No need to configure JWT_ACCESS_TOKEN_EXPIRATION_MINUTES here
